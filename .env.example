# Password for the /admin moderation page. Set to something strong.
ADMIN_PASSWORD=changeme

# Path to the SQLite database file. Defaults to ./data/meetups.db.
DATABASE_PATH=./data/meetups.db

# Cookie secret used to sign the admin session cookie. Set to a long random string.
SESSION_SECRET=replace-with-32-random-bytes
